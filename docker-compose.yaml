services:
 pg:
    image: postgres:18.0
    restart: always
    environment:
      POSTGRES_USER: "anglefar"
      POSTGRES_PASSWORD: "2434"
      POSTGRES_DB: "weather"
    volumes:
      - test_volume:/var/lib/postgresql     
    ports:
      - "5488:5432"
volumes:
  test_volume: {}
